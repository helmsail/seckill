server:
  port: 8082

spring:
  application:
    name: seckill-admin

dubbo:
  application:
    name: seckill-admin
    qos-enable: false
  registry:
    address: nacos://127.0.0.1:8848
  consumer:
    check: false

jwt:
  secret: seckill-jwt-secret-key-must-be-at-least-256-bits-long!!
  expiration: 86400000
